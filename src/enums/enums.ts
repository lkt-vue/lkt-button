export const enum ButtonType {
    button = 'button',
    submit = 'submit',
    reset = 'reset',
    content = 'content',
    switch = 'switch',
    hiddenSwitch = 'hidden-switch',
}

export const enum SplitType {
    lazy = 'lazy',
    ever = 'ever',
}